interface CustomButtonProps {
  name?: string;
  btnLink?: string;
  bgVarient?: "primary" | "secondary";
  icon?: React.ReactNode;
}

interface Testimonial {
  name: string;
  company: string;
  message: string;
  profile: string;
}
